// The brand, in one place. To rebrand again: change these two lines and
// run `grep -rn "Petalfall" src/ index.html vite.config.js docs/` for the
// handful of static literals (title/manifest/docs) that can't import JS.
// Storage keys ("nadya:"), the backup identifier ("nadya-dashboard") and
// the folder name are compatibility-critical and NEVER follow the brand.
export const BRAND = {
  name: "Petalfall",
  tagline: "a quiet space to grow",
};
